import {articles,href,articleUrl,categories,categorySlug} from '../lib/news';
export function GET({site}:{site:URL}){const paths=['','ultimas/','eleicoes/','sobre/','fontes/','politica-editorial/','arquivo/',...categories.map(c=>`categoria/${categorySlug(c)}/`),...new Set(articles.flatMap(a=>[`arquivo/${a.publishedAt.slice(0,4)}/`,`arquivo/${a.publishedAt.slice(0,4)}/${a.publishedAt.slice(5,7)}/`]))].map(href);const nodes=[...paths.map(p=>`<url><loc>${new URL(p,site).href}</loc></url>`),...articles.map(a=>`<url><loc>${new URL(articleUrl(a),site).href}</loc><lastmod>${a.updatedAt}</lastmod></url>`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${nodes.join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
